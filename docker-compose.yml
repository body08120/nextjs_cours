services:
  web:
    build: .
    working_dir: /app/nextjs-dashboard
    volumes:
      - .:/app
      - /app/nextjs-dashboard/node_modules
    ports:
      - "3000:3000"
    command: ["sh", "-c", "pnpm install && pnpm dev"]
